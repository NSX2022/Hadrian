package models;

/**
 * Enum representing all possible pages within the Hadrian application.
 * <p>
 * Used to reference a page without having a reference to the controller class.
 */
public enum Screens {
    HOME,
    CHATS,
    HELP,
    SETTINGS,
    CHAT
}
